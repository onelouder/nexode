# Nexode Requirements Extracted

Source: `docs/spec/master-spec.md`  
Section IDs: `docs/spec/spec-outline.md`  
Method: explicit requirements only; examples and rationale were excluded unless they state a requirement. Conflicts are marked, not resolved.

## Core and Architecture

| Requirement ID | Spec section ID | Type | Requirement statement | Observable success condition | Related domain entities | Architectural layers touched | Notes on ambiguity |
|---|---|---|---|---|---|---|---|
| `REQ-CORE-001` | `sec-01-executive-summary-core-philosophy` | capability | Nexode shall support agent-centric multi-project orchestration rather than file-centric editing. | A single session can coordinate work across multiple projects instead of only a single file-centric workspace. | `NexodeSession`; `Project`; `Agent`; `AgentSlot` | Layer 1; Layer 3 | — |
| `REQ-CORE-002` | `sec-01-executive-summary-core-philosophy` | capability | Nexode shall enable one developer to manage 10-15 parallel autonomous coding agents across 10-15 separate codebases and tasks. | A session can actively manage 10-15 concurrent agents distributed across 10-15 projects. | `NexodeSession`; `Project`; `Agent` | Layer 1; Layer 3 | Also repeated in Phase 1 scale goals. |
| `REQ-CORE-003` | `sec-01-core-reframing-v2` | invariant | The primary use case shall be 10-15 agents distributed across 10-15 separate projects, not 15 agents on one codebase. | Product flows, schema, and UI grouping all center on multiple projects. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1; Layer 3 | — |
| `REQ-CORE-004` | `sec-01-executive-summary-core-philosophy` | constraint | Agents shall be isolated in Git worktrees to execute separate, loosely coupled tasks. | Each active slot in a git-backed project has its own isolated worktree. | `Project`; `AgentSlot`; `Worktree`; `Agent` | Layer 0; Layer 1 | — |
| `REQ-CORE-005` | `sec-01-executive-summary-core-philosophy` | capability | Nexode shall provide rich visual orchestration, shared memory, and a central control plane across multiple repositories. | UI surfaces expose cross-project telemetry and control while shared memory and orchestration operate across repos. | `Project`; `Agent`; `Shared Memory Bus`; `OrchestratorAgent` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-CORE-006` | `sec-01-design-principles` | constraint | Nexode shall follow an extension-first, fork-never-for-now architecture; Phase 5 is conditional. | Delivery proceeds through daemon plus extension/TUI unless the explicit Phase 5 trigger occurs. | `Rendering Shell`; `Hypervisor` | Layer 1; Layer 3 | — |
| `REQ-CORE-007` | `sec-01-design-principles` | invariant | The Rust hypervisor daemon shall be the product core; the TUI and VS Code extension shall be rendering shells. | Headless daemon operation remains functional even when no UI is attached. | `Hypervisor`; `Rendering Shell` | Layer 1; Layer 3 | — |
| `REQ-CORE-008` | `sec-01-design-principles` | invariant | Multi-project shall be the primitive; single-project shall behave as a degenerate case. | Schema, cost tracking, and UI grouping work for multi-project sessions and also accept single-project sessions. | `NexodeSession`; `Project` | Layer 1; Layer 3 | — |
| `REQ-CORE-009` | `sec-01-design-principles` | capability | Nexode shall treat agent CLIs as model-agnostic fungible compute through a harness abstraction. | A session can mix Claude Code, Codex, Gemini CLI, and local models under one orchestration model. | `Agent`; `Agent Harness abstraction` | Layer 1; Layer 3 | — |
| `REQ-ARCH-001` | `sec-02-system-architecture-4-layer-model` | invariant | The system shall be organized into four cleanly decoupled layers. | Architecture documentation and implementation boundaries match Layer 0-3 responsibilities. | `Substrate`; `Hypervisor`; `gRPC Bridge`; `Rendering Shell` | Layer 0; Layer 1; Layer 2; Layer 3 | — |
| `REQ-ARCH-002` | `sec-02-system-architecture-4-layer-model` | capability | Layer 1 shall be a standalone Rust binary using the tokio async runtime. | The daemon runs as an independent Rust process built around tokio. | `Hypervisor` | Layer 1 | — |
| `REQ-ARCH-003` | `sec-02-system-architecture-4-layer-model` | capability | Layer 1 shall manage process lifecycle, multi-repo Git worktree orchestration, vector memory, token accounting, the DAG workflow engine, and the OrchestratorAgent control loop. | The daemon exposes all named subsystems and they function without a UI dependency. | `Agent`; `Worktree`; `Token Accountant`; `Workflow DAG Engine`; `OrchestratorAgent`; `Vector Memory Bus` | Layer 1 | — |
| `REQ-ARCH-004` | `sec-02-system-architecture-4-layer-model` | capability | Layer 2 shall provide high-throughput bidirectional IPC over a Unix socket or named pipe. | A renderer can connect over a Unix socket on Unix-like systems or a named pipe on Windows. | `Hypervisor`; `Rendering Shell`; `HypervisorEvent`; `OperatorCommand` | Layer 2 | — |
| `REQ-ARCH-005` | `sec-02-system-architecture-4-layer-model` | capability | Layer 2 shall stream agent output tokens, telemetry, and state mutations directly to the renderer. | Connected renderers receive live output, telemetry, and state events from the daemon. | `Agent`; `HypervisorEvent` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-ARCH-006` | `sec-02-system-architecture-4-layer-model` | capability | Layer 3 shall include a VS Code extension pack with modular UI surfaces, and the TUI shall be an alternative Layer 3 renderer. | Both a VS Code shell and a TUI can render daemon state. | `Rendering Shell`; `TUI` | Layer 3 | — |
| `REQ-ARCH-007` | `sec-02-key-architectural-property` | invariant | The daemon shall be completely decoupled from the UI. | The daemon can run without a local renderer and renderers can attach later. | `Hypervisor`; `Rendering Shell` | Layer 1; Layer 3 | — |
| `REQ-ARCH-008` | `sec-02-key-architectural-property` | capability | Multiple rendering shells shall be able to connect to the same daemon simultaneously. | Two or more renderers can subscribe to the same daemon session at the same time. | `Rendering Shell`; `Hypervisor` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-ARCH-009` | `sec-02-layer-1-rust-daemon-core-architecture` | constraint | The daemon core shall use a central message-passing architecture based on `tokio::sync::mpsc`, with no shared mutable state. | Core loop coordination occurs through channels rather than shared mutable state. | `Hypervisor`; `OperatorCommand`; `HypervisorEvent` | Layer 1 | — |
| `REQ-ARCH-010` | `sec-02-layer-1-rust-daemon-core-architecture` | capability | The daemon shall run a gRPC server loop that listens for `OperatorCommand` messages from connected UIs and pushes commands to the Core Engine via an mpsc channel. | UI commands arrive through gRPC and are handed to the core loop through the channel path described. | `OperatorCommand`; `Core Engine` | Layer 1; Layer 2 | — |
| `REQ-ARCH-011` | `sec-02-layer-1-rust-daemon-core-architecture` | capability | The daemon shall run up to 15 async agent process runners, one per active CLI agent. | Up to 15 active agents each have an async runner reading stdout/stderr and producing telemetry. | `Agent` | Layer 1 | — |
| `REQ-ARCH-012` | `sec-02-layer-1-rust-daemon-core-architecture` | capability | The Core Engine and Orchestrator loop shall receive UI commands, agent outputs, and cron triggers, update SQLite state, and broadcast `HypervisorEvent` payloads. | State changes from commands, outputs, and ticks appear in SQLite and in the event stream. | `Core Engine`; `OrchestratorAgent`; `HypervisorEvent`; `Token Accountant` | Layer 1; Layer 2 | — |
| `REQ-ARCH-013` | `sec-02-daemon-subsystem-breakdown` | capability | The Agent Process Manager shall spawn, monitor, and terminate fungible CLI agent processes, including heartbeat and watchdog handling. | Agent processes can be started, supervised, timed out, and terminated by the daemon. | `Agent` | Layer 1 | — |
| `REQ-ARCH-014` | `sec-02-daemon-subsystem-breakdown` | capability | The Git Worktree Orchestrator shall create, assign, merge, and garbage-collect isolated git worktrees across multiple repositories. | Worktrees are created and later cleaned up for active git-backed slots across repos. | `Project`; `AgentSlot`; `Worktree` | Layer 0; Layer 1 | — |
| `REQ-ARCH-015` | `sec-02-daemon-subsystem-breakdown` | deferred | The Scoped Context Compiler shall query the Vector Memory Bus to build targeted, role-specific prompts for blank agents in Phase 4. | Phase 4 implementation produces role-targeted context slices before dispatch. | `Scoped Context Compiler`; `Vector Memory Bus`; `Agent` | Layer 1 | Phase-gated to Phase 4. |
| `REQ-ARCH-016` | `sec-02-daemon-subsystem-breakdown` | capability | The Workflow DAG Engine shall manage Kanban columns, parse `session.yaml` task slots, and track task completion states. | Task dependencies and column/state changes are represented in the workflow engine. | `TaskNode`; `AgentSlot`; `Workflow DAG Engine` | Layer 1 | — |
| `REQ-ARCH-017` | `sec-02-daemon-subsystem-breakdown` | capability | The Token Accountant shall track per-agent, per-slot, per-project, and per-session token usage and cost estimates in SQLite. | Token and cost totals are queryable at all four levels. | `Agent`; `AgentSlot`; `Project`; `NexodeSession`; `Token Accountant` | Layer 1 | — |
| `REQ-ARCH-018` | `sec-02-daemon-subsystem-breakdown` | constraint | The Session Config Manager shall parse `session.yaml v2` with strict validation, include directives, `.nexode.yaml` merging, and v1 fallback support. | Invalid config is rejected; includes and repo-local merge work; v1 config still loads. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1 | Defaults cascade details conflict with missing project-level defaults in the schema. |

## Domain Model and Protocol

| Requirement ID | Spec section ID | Type | Requirement statement | Observable success condition | Related domain entities | Architectural layers touched | Notes on ambiguity |
|---|---|---|---|---|---|---|---|
| `REQ-DOM-001` | `sec-03-01-domain-model-hierarchy` | invariant | The domain hierarchy shall be `NexodeSession > Project > AgentSlot > Agent`. | Runtime state and UI references preserve the stated ownership hierarchy. | `NexodeSession`; `Project`; `AgentSlot`; `Agent` | Layer 1; Layer 3 | — |
| `REQ-DOM-002` | `sec-03-01-domain-model-hierarchy` | invariant | `AgentSlot` shall be the stable identity that survives agent crashes. | A slot persists across agent replacement and remains the UI and accounting anchor. | `AgentSlot`; `Agent` | Layer 1; Layer 3 | — |
| `REQ-DOM-003` | `sec-03-01-domain-model-hierarchy` | invariant | A `Project` shall map to one Git repository, or to no repository for non-git tasks. | Sessions can include both git-backed and non-git projects. | `Project` | Layer 1 | — |
| `REQ-DOM-004` | `sec-03-01-domain-model-hierarchy` | invariant | An `Agent` shall be an ephemeral CLI process bound to a slot and replaceable without changing slot identity. | Replacing an agent does not require creating a new slot. | `Agent`; `AgentSlot` | Layer 1 | — |
| `REQ-DOM-005` | `sec-03-01-domain-model-hierarchy` | invariant | A `TaskNode` may map 1:1 to a slot or be subdivided. | The task DAG can represent both one-slot tasks and subdivided tasks. | `TaskNode`; `AgentSlot` | Layer 1; Layer 3 | — |
| `REQ-DOM-006` | `sec-03-01-domain-model-hierarchy` | invariant | There shall be one active `Worktree` per active slot per git-backed project. | Each active git-backed slot has its own isolated worktree path. | `Worktree`; `AgentSlot`; `Project` | Layer 0; Layer 1 | — |
| `REQ-DOM-007` | `sec-03-02-fungible-workers-agentslot-abstraction` | capability | At the OS layer, agents shall be generic spawned CLI processes that can be terminated and replaced into the same slot on crash or hang. | A crashed or hung agent is replaced without changing slot identity. | `Agent`; `AgentSlot` | Layer 0; Layer 1 | — |
| `REQ-DOM-008` | `sec-03-02-fungible-workers-agentslot-abstraction` | invariant | Slot ID, cost history, worktree, and task context shall be preserved across agent replacement. | Recovered slots keep the same slot ID, accounting lineage, and worktree. | `AgentSlot`; `Worktree`; `Token Accountant` | Layer 1 | — |
| `REQ-DOM-009` | `sec-03-02-fungible-workers-agentslot-abstraction` | deferred | In Phase 4, an agent shall gain its task-specific identity when the daemon compiles and injects a role-appropriate context slice at slot assignment time. | Phase 4 dispatches include compiled structural and semantic context specific to the slot. | `Agent`; `AgentSlot`; `Scoped Context Compiler` | Layer 1 | Phase-gated to Phase 4. |
| `REQ-DOM-010` | `sec-03-02-fungible-workers-agentslot-abstraction` | capability | Crash recovery shall emit a `SlotAgentSwapped` event. | A swap event appears in the event stream whenever a slot gets a replacement agent. | `AgentSlot`; `Agent`; `SlotAgentSwapped` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-DOM-011` | `sec-03-02-fungible-workers-agentslot-abstraction` | constraint | Each agent shall operate in a completely isolated Git worktree so that no file-level locking is needed. | Concurrent agents do not require file locks to avoid write collisions. | `Agent`; `Worktree` | Layer 0; Layer 1 | — |
| `REQ-DOM-012` | `sec-03-03-shared-memory-bus` | deferred | The Shared Memory Bus shall be implemented as a three-layer memory architecture in Phase 4: Codebase Graph, Decision Log, and Session State. | Phase 4 exposes all three named memory layers. | `Shared Memory Bus`; `Codebase Graph`; `Decision Log`; `Session State` | Layer 1 | Phase-gated to Phase 4. |
| `REQ-DOM-013` | `sec-03-03-shared-memory-bus` | deferred | The Codebase Graph shall store embeddings of AST nodes, dependency relationships, and import maps for context compilation. | Structural code search can retrieve relevant AST signatures for a task. | `Codebase Graph`; `Scoped Context Compiler` | Layer 1 | Phase-gated to Phase 4. |
| `REQ-DOM-014` | `sec-03-03-shared-memory-bus` | performance | The Codebase Graph query path shall reduce context window bloat by 20-40%. | Token/context usage measurements show a 20-40% reduction attributable to structural retrieval. | `Codebase Graph`; `Scoped Context Compiler` | Layer 1 | Also restated as a Phase 4 success target. |
| `REQ-DOM-015` | `sec-03-03-shared-memory-bus` | capability | The Decision Log shall be an append-only log of reasoning traces, architectural decisions, and rejected alternatives. | Later agents can query preserved historical decisions from prior work. | `Decision Log`; `Agent` | Layer 1 | — |
| `REQ-DOM-016` | `sec-03-03-shared-memory-bus` | invariant | Session State shall store active tasks, agent assignments, and pending approvals in SQLite and shall be cleared when the session ends. | Active session data exists during a run and is cleared on session end. | `Session State`; `TaskNode`; `Agent`; `AgentSlot` | Layer 1 | — |
| `REQ-PROTO-001` | `sec-03-04-hypervisor-proto-v2-contract` | invariant | `hypervisor.proto v2` shall be the absolute source of truth for the daemon-to-renderer gRPC contract. | Renderer and daemon interoperability is defined by the v2 proto contract. | `Hypervisor`; `HypervisorEvent`; `OperatorCommand` | Layer 1; Layer 2; Layer 3 | Conflicts later with a Phase 1 statement about `FullStateSnapshot` fields. |
| `REQ-PROTO-002` | `sec-03-04-hypervisor-proto-v2-contract` | capability | v2 of the protocol shall add `Project`, `AgentSlot`, `ProjectBudgetAlert`, and `SlotAgentSwapped`. | Those entities exist in the contract and appear in runtime state/events. | `Project`; `AgentSlot`; `ProjectBudgetAlert`; `SlotAgentSwapped` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-003` | `sec-03-04-service-enums` | capability | The `Hypervisor` service shall expose `SubscribeEvents`, `DispatchCommand`, and `GetFullState` RPCs. | A client can subscribe to events, dispatch a command, and request a full state snapshot. | `Hypervisor`; `SubscribeRequest`; `OperatorCommand`; `FullStateSnapshot` | Layer 1; Layer 2 | — |
| `REQ-PROTO-004` | `sec-03-04-service-enums` | invariant | `AgentState` shall distinguish `INIT`, `IDLE`, `PLANNING`, `EXECUTING`, `REVIEW`, `BLOCKED`, and `TERMINATED`. | Runtime state transitions and UI status display can represent all listed agent states. | `Agent`; `AgentState` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-005` | `sec-03-04-service-enums` | invariant | `TaskStatus` shall distinguish `PENDING`, `WORKING`, `REVIEW`, `DONE`, `PAUSED`, and `ARCHIVED`. | The task DAG and command paths can represent all listed task states. | `TaskNode`; `TaskStatus` | Layer 1; Layer 2; Layer 3 | Conflicts with the UI column `Merged`, which is not in the enum. |
| `REQ-PROTO-006` | `sec-03-04-service-enums` | invariant | `AgentMode` shall distinguish `NORMAL`, `PLAN`, and `FULL_AUTO`. | Mode changes and serialization can represent all listed protocol modes. | `Agent`; `AgentMode` | Layer 1; Layer 2; Layer 3 | Ambiguous mapping: config/autonomy text uses `manual`, not `NORMAL`. |
| `REQ-PROTO-007` | `sec-03-04-entity-models` | capability | The `Project` message shall carry project identity, display name, repository path, color, tags, budget thresholds, current cost, and slot list. | A full state snapshot includes per-project identity, metadata, budget, cost, and slots. | `Project`; `AgentSlot` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-008` | `sec-03-04-entity-models` | capability | The `AgentSlot` message shall carry slot identity, owning project, task, mode, branch, current agent, worktree, total tokens, and total cost. | Slot state returned by the daemon includes all listed fields. | `AgentSlot`; `Agent`; `Worktree` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-009` | `sec-03-04-entity-models` | capability | The `Agent` message shall carry agent identity, display name, current role, state, mode, slot binding, worktree binding, tokens consumed, estimated cost, and tokens/sec. | Live agent telemetry and state include all listed agent fields. | `Agent`; `AgentSlot`; `Worktree` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-010` | `sec-03-04-entity-models` | capability | The `TaskNode` message shall carry task identity, title, description, status, assigned agent, owning project, and dependency IDs. | The task DAG contains all listed task metadata and dependency links. | `TaskNode`; `Agent`; `Project` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-011` | `sec-03-04-entity-models` | capability | The `Worktree` message shall carry worktree identity, absolute path, branch name, and conflict risk. | Worktree state presented to UIs includes path, branch, and risk. | `Worktree` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-012` | `sec-03-04-entity-models` | capability | `FullStateSnapshot` shall include `projects`, `task_dag`, `total_session_cost`, and `session_budget_max_usd`. | A renderer can reconstruct global state from those snapshot fields. | `FullStateSnapshot`; `Project`; `TaskNode`; `NexodeSession` | Layer 1; Layer 2; Layer 3 | Conflicts with `sec-09-weeks-3-4-grpc-state`, which also says `agent_slots[]` is included. |
| `REQ-PROTO-013` | `sec-03-04-events-commands` | capability | `HypervisorEvent` shall carry `event_id`, `timestamp_ms`, `barrier_id`, and one of the defined payload variants. | Every emitted event includes a stable envelope and a typed payload. | `HypervisorEvent` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-014` | `sec-03-04-events-commands` | capability | The event stream shall support `AgentStateChanged`, `AgentTelemetryUpdated`, `TaskStatusChanged`, `UncertaintyFlagTriggered`, `WorktreeStatusChanged`, `ProjectBudgetAlert`, and `SlotAgentSwapped`. | Subscribers can receive all listed event variants at runtime. | `Agent`; `TaskNode`; `Worktree`; `ProjectBudgetAlert`; `SlotAgentSwapped` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-015` | `sec-03-04-events-commands` | constraint | `ProjectBudgetAlert.hard_kill` shall be `true` at a max-budget breach and `false` at a warning threshold. | Alert consumers can distinguish soft warning from hard budget breach via the boolean. | `ProjectBudgetAlert`; `Project` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-016` | `sec-03-04-events-commands` | capability | `SlotAgentSwapped.reason` shall support `crash_recovery`, `manual_reassign`, and `initial`. | Swap events expose one of the named reasons. | `SlotAgentSwapped`; `AgentSlot`; `Agent` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-PROTO-017` | `sec-03-04-events-commands` | capability | `OperatorCommand` shall support pausing, resuming, killing agents, moving tasks, assigning tasks, setting agent mode, dispatching chat, and killing a project. | The daemon accepts and handles all listed command variants. | `OperatorCommand`; `Agent`; `TaskNode`; `Project` | Layer 1; Layer 2 | Chat- and slot-target semantics are less formal than agent-target semantics. |
| `REQ-PROTO-018` | `sec-03-04-events-commands` | capability | `KillProject` shall stop all agents in the named project. | Issuing `KillProject` terminates every active agent for that project. | `KillProject`; `Project`; `Agent` | Layer 1; Layer 2 | — |
| `REQ-PROTO-019` | `sec-03-04-design-highlights` | UX | `barrier_id` shall synchronize React Webview updates so state is not torn across panels. | All active webviews render a given state barrier consistently before moving to the next. | `HypervisorEvent`; `EventBus`; `Rendering Shell` | Layer 2; Layer 3 | — |
| `REQ-PROTO-020` | `sec-03-04-design-highlights` | capability | `ChatDispatch` shall wrap natural language from the VS Code ChatBar and route it to the OrchestratorAgent for parsing. | Free-form chat commands appear as `ChatDispatch` and are parsed by the orchestrator. | `ChatDispatch`; `OrchestratorAgent` | Layer 1; Layer 2; Layer 3 | — |

## Session Configuration

| Requirement ID | Spec section ID | Type | Requirement statement | Observable success condition | Related domain entities | Architectural layers touched | Notes on ambiguity |
|---|---|---|---|---|---|---|---|
| `REQ-CFG-001` | `sec-04-session-configuration-session-yaml-v2` | capability | `session.yaml v2` shall be redesigned for the multi-project model. | A single session file can define multiple projects and slots. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-002` | `sec-04-new-in-v2-e-004` | capability | `session.yaml v2` shall include a defaults block for DRY configuration. | Shared model, mode, timeout, or provider config can be set once and inherited. | `NexodeSession`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-003` | `sec-04-new-in-v2-e-004` | capability | `session.yaml v2` shall support include directives for splitting large sessions across files. | A session can reference external YAML fragments and load successfully. | `NexodeSession`; `Project` | Layer 1 | — |
| `REQ-CFG-004` | `sec-04-new-in-v2-e-004` | capability | `session.yaml v2` shall support per-project `.nexode.yaml` overrides. | Repo-local config is discovered and merged into matching projects. | `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-005` | `sec-04-new-in-v2-e-004` | capability | `session.yaml v2` shall support project tags for filtering. | Project definitions can carry tags and UI/filter logic can use them. | `Project` | Layer 1; Layer 3 | — |
| `REQ-CFG-006` | `sec-04-new-in-v2-e-004` | capability | `session.yaml v2` shall include a model pricing table. | The config file can define token pricing per model. | `Token Accountant`; `NexodeSession` | Layer 1 | — |
| `REQ-CFG-007` | `sec-04-01-design-principles` | UX | A session with one project and one agent should require fewer than 10 lines of YAML. | A minimal single-project single-agent session definition fits within 10 lines. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-008` | `sec-04-01-design-principles` | constraint | Global `~/.nexode/session.yaml` shall define the portfolio and repo-local `.nexode.yaml` files shall define slots for matching repos. | A global session plus repo-local slot overlays load as a single merged configuration. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-009` | `sec-04-01-design-principles` | constraint | The daemon shall merge global and repo-local configuration, with repo-local values overriding global defaults. | Effective config reflects repo-local overrides where both define the same setting. | `Project`; `AgentSlot` | Layer 1 | Ambiguous because the spec also claims a `session > project > slot` defaults cascade without a formal project-level defaults block. |
| `REQ-CFG-010` | `sec-04-01-design-principles` | capability | A YAML file without `projects[]` shall be treated as a single implicit project when v1 compatibility applies. | A v1-style file loads as one implicit project. | `NexodeSession`; `Project` | Layer 1 | — |
| `REQ-CFG-011` | `sec-04-01-design-principles` | constraint | Project and slot IDs shall be short kebab-case strings chosen by the user rather than UUIDs. | Config validation accepts short kebab-case IDs and does not require UUIDs. | `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-012` | `sec-04-02-session-model-config` | constraint | `version` shall identify the schema version; `2.0` denotes the multi-project schema and `1.0` denotes the legacy single-project schema. | Loading behavior changes according to the declared version. | `NexodeSession` | Layer 1 | — |
| `REQ-CFG-013` | `sec-04-03-session-level-fields` | constraint | `session.name` shall be required. | Validation fails if `session.name` is missing. | `NexodeSession` | Layer 1 | — |
| `REQ-CFG-014` | `sec-04-03-session-level-fields` | constraint | `session.budget.max_usd` shall be optional and shall default to unlimited. | A session without `max_usd` has no session hard ceiling. | `NexodeSession` | Layer 1 | — |
| `REQ-CFG-015` | `sec-04-03-session-level-fields` | constraint | `session.budget.warn_usd` shall be optional and shall default to 80% of `max_usd`. | A missing warning threshold resolves to 80% of the session max. | `NexodeSession` | Layer 1 | — |
| `REQ-CFG-016` | `sec-04-03-session-level-fields` | constraint | `session.defaults.model` shall be optional and shall default to `claude-code`. | Slots without an explicit model resolve to `claude-code`. | `AgentSlot`; `NexodeSession` | Layer 1 | — |
| `REQ-CFG-017` | `sec-04-03-session-level-fields` | constraint | `session.defaults.mode` shall be optional and shall default to `plan`. | Slots without an explicit mode resolve to `plan`. | `AgentSlot`; `NexodeSession` | Layer 1 | Mapping to proto `AGENT_MODE_NORMAL` remains ambiguous for `manual`. |
| `REQ-CFG-018` | `sec-04-03-session-level-fields` | constraint | `session.defaults.timeout_minutes` shall be optional and shall default to 120. | Idle timeout resolves to 120 minutes when omitted. | `Agent`; `NexodeSession` | Layer 1 | — |
| `REQ-CFG-019` | `sec-04-02-session-model-config` | capability | `session.defaults.provider_config` shall support provider-specific credential/config entries. | Provider config values can be declared for configured providers. | `NexodeSession`; `Agent` | Layer 1 | The field is shown in the annotated schema example rather than the field reference. |
| `REQ-CFG-020` | `sec-04-02-session-model-config` | capability | The `models` section shall support per-model pricing rates per 1M tokens. | Cost estimation can read model-specific input/output and cache-read rates. | `Token Accountant`; `NexodeSession` | Layer 1 | — |
| `REQ-CFG-021` | `sec-04-03-project-level-fields` | constraint | `Project.id` shall be required. | Validation fails if a project has no ID. | `Project` | Layer 1 | — |
| `REQ-CFG-022` | `sec-04-03-project-level-fields` | constraint | `Project.repo` shall be optional and may be omitted for non-git tasks. | Non-git projects load without a repo path. | `Project` | Layer 1 | — |
| `REQ-CFG-023` | `sec-04-03-project-level-fields` | constraint | `Project.display_name` shall be optional and shall default to the project ID. | Projects without a display name render with the ID as the human name. | `Project` | Layer 1; Layer 3 | — |
| `REQ-CFG-024` | `sec-04-03-project-level-fields` | constraint | `Project.color` shall be optional and shall auto-assign when omitted. | A project without a color still renders with a group color. | `Project` | Layer 1; Layer 3 | — |
| `REQ-CFG-025` | `sec-04-03-project-level-fields` | capability | `Project.tags[]` shall be optional filterable labels. | Projects can carry filter tags such as `client` or `oss`. | `Project` | Layer 1; Layer 3 | — |
| `REQ-CFG-026` | `sec-04-03-project-level-fields` | constraint | `Project.budget.max_usd` shall be optional and shall default to unlimited. | A project without a budget cap does not hard-stop on project spend. | `Project` | Layer 1 | — |
| `REQ-CFG-027` | `sec-04-03-project-level-fields` | capability | `Project.include` shall allow a project definition to come from an external YAML file. | A project entry can resolve through an `include` path. | `Project` | Layer 1 | — |
| `REQ-CFG-028` | `sec-04-03-slot-level-fields` | constraint | `AgentSlot.id` shall be required and unique within a project. | Validation fails if a slot has no ID or duplicates another slot ID in the same project. | `AgentSlot`; `Project` | Layer 1 | — |
| `REQ-CFG-029` | `sec-04-03-slot-level-fields` | constraint | `AgentSlot.task` shall be required as a natural-language task description. | Validation fails if a slot has no task text. | `AgentSlot` | Layer 1 | — |
| `REQ-CFG-030` | `sec-04-03-slot-level-fields` | constraint | `AgentSlot.model` shall be optional and shall inherit the session default when omitted. | Omitted slot model resolves to the configured default model. | `AgentSlot`; `NexodeSession` | Layer 1 | — |
| `REQ-CFG-031` | `sec-04-03-slot-level-fields` | constraint | `AgentSlot.mode` shall be optional, inherit its default when omitted, and accept `manual`, `plan`, or `full_auto`. | Slot mode validation accepts only the named mode values. | `AgentSlot` | Layer 1 | Ambiguous relative to proto `AGENT_MODE_NORMAL`. |
| `REQ-CFG-032` | `sec-04-03-slot-level-fields` | constraint | `AgentSlot.branch` shall be optional and shall default to `agent/{id}`. | Slots without an explicit branch name get an `agent/{slot-id}` branch. | `AgentSlot`; `Worktree` | Layer 0; Layer 1 | — |
| `REQ-CFG-033` | `sec-04-03-slot-level-fields` | constraint | `context.include[]` shall be optional and shall default to `all`. | A slot with no include globs considers the full repo context. | `AgentSlot` | Layer 1 | — |
| `REQ-CFG-034` | `sec-04-03-slot-level-fields` | constraint | `context.exclude[]` shall be optional and shall default to `none`. | A slot with no exclude globs has no excluded paths. | `AgentSlot` | Layer 1 | — |
| `REQ-CFG-035` | `sec-04-04-per-project-nexode-yaml` | capability | A repo-local `.nexode.yaml` shall be discovered at repo root and merged into the matching project entry. | Starting a session with a matching repo root applies the repo-local overlay automatically. | `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-036` | `sec-04-04-per-project-nexode-yaml` | constraint | Slots defined in repo-local `.nexode.yaml` shall be added to the session project, and matching slot IDs shall take precedence locally. | Added repo-local slots appear in the effective config; duplicate IDs resolve to repo-local definitions. | `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-CFG-037` | `sec-04-05-backward-compatibility-v1-schema` | capability | If the daemon encounters a YAML file without `projects` or with `version: "1.0"`, it shall wrap the file in an implicit project named `default`. | A legacy config loads under a generated `default` project without manual edits. | `NexodeSession`; `Project` | Layer 1 | — |

## UX and Orchestration

| Requirement ID | Spec section ID | Type | Requirement statement | Observable success condition | Related domain entities | Architectural layers touched | Notes on ambiguity |
|---|---|---|---|---|---|---|---|
| `REQ-UX-001` | `sec-05-ui-ux-surfaces-command-center` | UX | The UI shall provide a rich terminal interface in Phase 2 and standard VS Code Webview panels in Phase 3. | Both the TUI and VS Code renderer exist in the phases stated. | `Rendering Shell`; `TUI` | Layer 3 | — |
| `REQ-UX-002` | `sec-05-ui-ux-surfaces-command-center` | capability | Both rendering shells shall consume the same gRPC event stream from the daemon. | The same daemon state/events drive both the TUI and VS Code shells. | `HypervisorEvent`; `Rendering Shell` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-UX-003` | `sec-05-v2-changes-e-006` | UX | UI surfaces shall organize agents by project group. | Agents render grouped under project headers by default. | `Project`; `Agent` | Layer 3 | — |
| `REQ-UX-004` | `sec-05-v2-changes-e-006` | UX | The UI shall support `Project Groups`, `Flat View`, and `Focus View`. | Operators can switch among the three named view modes. | `Project`; `Agent` | Layer 3 | — |
| `REQ-UX-005` | `sec-05-v2-changes-e-006` | deferred | Monitor assignment shall be deferred to Phase 4+. | No automatic monitor assignment is required in earlier phases. | `Rendering Shell` | Layer 3 | Tension with Phase 3 multi-monitor wording; likely manual placement vs automatic assignment. |
| `REQ-UX-006` | `sec-05-v2-changes-e-006` | UX | Idle agent reallocation shall be manual in Phase 2 and auto-suggested in Phase 3+. | Phase 2 requires explicit operator action; Phase 3+ can suggest reallocations. | `Agent`; `AgentSlot`; `OrchestratorAgent` | Layer 1; Layer 3 | — |
| `REQ-UX-007` | `sec-05-synapse-telemetry-grid` | UX | The default Synapse Telemetry Grid view shall group agents by project, show project name, agent count, and per-project cost, and allow group collapse. | Operators can collapse/expand project groups and see the named group metrics. | `Project`; `Agent` | Layer 3 | — |
| `REQ-UX-008` | `sec-05-synapse-telemetry-grid` | UX | `Flat View` shall show all agents in one grid sorted by state, with executing agents first and idle agents last. | Switching to flat view changes grouping and ordering as described. | `Agent` | Layer 3 | — |
| `REQ-UX-009` | `sec-05-synapse-telemetry-grid` | UX | `Focus View` shall expand one project to fill the grid and collapse other projects into a compact sidebar list. | Focusing a project changes layout to one large project plus compressed others. | `Project`; `Agent` | Layer 3 | — |
| `REQ-UX-010` | `sec-05-synapse-telemetry-grid` | UX | Each grid cell shall display streaming terminal/diff output, token velocity, elapsed time, cost, and one-click pause/resume/kill/reassign actions. | A live agent cell exposes all listed telemetry and controls. | `Agent`; `AgentSlot` | Layer 2; Layer 3 | — |
| `REQ-UX-011` | `sec-05-synapse-telemetry-grid` | UX | Sidebar mode shall show agent avatars, active task names, and status indicators. | The VS Code sidebar renders a compressed agent list with the named fields. | `Agent`; `TaskNode` | Layer 3 | — |
| `REQ-UX-012` | `sec-05-synapse-telemetry-grid` | UX | Maximized mode shall use a `WebviewPanel`, support 1x1 through 3x3 grid layouts, and be draggable to a second monitor. | The grid can be popped out and resized within the named range. | `Rendering Shell` | Layer 3 | Manual second-monitor drag is explicit; automatic monitor placement is deferred. |
| `REQ-UX-013` | `sec-05-macro-kanban-board-task-queue` | UX | The Macro Kanban Board shall be a full-screen `WebviewPanel` with a drag-and-drop DAG dependency map. | Operators can rearrange task dependencies through the Kanban/DAG surface. | `TaskNode`; `Workflow DAG Engine` | Layer 3 | — |
| `REQ-UX-014` | `sec-05-macro-kanban-board-task-queue` | UX | The Macro Kanban Board shall expose the columns `Pending`, `Working`, `Done`, `Merged`, `Paused`, and `Archived`. | The board renders those six columns. | `TaskNode` | Layer 3 | Conflicts with `TaskStatus`, which does not define `Merged`. |
| `REQ-UX-015` | `sec-05-macro-kanban-board-task-queue` | UX | Expanded task cards shall show worktree path, branch name, conflict risk score, and token cost. | Opening a card reveals all listed details. | `TaskNode`; `Worktree`; `AgentSlot` | Layer 1; Layer 3 | — |
| `REQ-UX-016` | `sec-05-macro-kanban-board-task-queue` | UX | Phase 2 shall support project filtering by dropdown selector and Phase 3+ shall add cross-project swim lanes. | Project filtering exists in the TUI/board first; swim lanes appear later. | `Project`; `TaskNode` | Layer 3 | — |
| `REQ-UX-017` | `sec-05-universal-command-chat` | UX | Universal Command Chat shall be registered as a chat participant in VS Code's native ChatBar. | `@nexode` chat routing is available in the VS Code chat surface. | `ChatParticipant`; `OrchestratorAgent` | Layer 3 | — |
| `REQ-UX-018` | `sec-05-universal-command-chat` | capability | Users shall be able to route instructions to specific slots or agents through chat. | Slot- or agent-targeted chat commands are accepted and routed. | `Agent`; `AgentSlot`; `ChatDispatch` | Layer 1; Layer 2; Layer 3 | Ambiguous because the formal structured API is primarily agent-centric. |
| `REQ-UX-019` | `sec-05-merge-choreography` | UX | Merge Choreography shall visualize the Git worktree merge queue per project. | Operators can inspect a per-project merge queue in the UI. | `Project`; `Worktree` | Layer 1; Layer 3 | — |
| `REQ-UX-020` | `sec-05-merge-choreography` | deferred | Merge Choreography shall show structural conflict risk scores, with AST-based scoring in Phase 4. | Conflict-risk values appear, and Phase 4 upgrades them to AST-based scoring. | `Worktree`; `Predictive Conflict Routing` | Layer 1; Layer 3 | — |
| `REQ-UX-021` | `sec-05-merge-choreography` | UX | Merge Choreography shall facilitate human-in-the-loop merge approvals. | The UI includes an approval path before merges are finalized. | `Worktree`; `TaskNode` | Layer 3 | The entity in `REVIEW` is not fully formalized for worktrees. |
| `REQ-UX-022` | `sec-05-status-bar-hud` | UX | The Status Bar HUD shall show active agent count, aggregate token velocity, total session cost, top 3 per-project cost breakdown, and a developer fatigue meter. | The VS Code footer updates with all listed metrics. | `Agent`; `Project`; `NexodeSession` | Layer 1; Layer 3 | — |
| `REQ-ORCH-001` | `sec-06-orchestration-autonomy-orchestratoragent` | capability | The OrchestratorAgent shall run as a continuous control-plane loop within the Rust daemon. | A long-lived observer/orchestrator loop exists during daemon runtime. | `OrchestratorAgent`; `Hypervisor` | Layer 1 | — |
| `REQ-ORCH-002` | `sec-06-orchestration-autonomy-orchestratoragent` | constraint | In the multi-project model, orchestration shall be per-project until Phase 3+ and shall not attempt cross-project intelligence earlier. | Phase 0-2 orchestration decisions stay within project boundaries. | `OrchestratorAgent`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-ORCH-003` | `sec-06-auto-dispatch` | capability | The Orchestrator shall monitor slot states and decide whether to respawn an agent or mark a slot idle when work completes or the agent crashes. | Completed or crashed slots transition through explicit idle or recovery behavior. | `OrchestratorAgent`; `AgentSlot`; `Agent` | Layer 1 | — |
| `REQ-ORCH-004` | `sec-06-auto-dispatch` | capability | In `full_auto`, completed slots with remaining sub-tasks shall be automatically re-dispatched. | A `full_auto` slot continues onto remaining sub-work without waiting for the operator. | `AgentSlot`; `OrchestratorAgent` | Layer 1 | — |
| `REQ-ORCH-005` | `sec-06-autonomy-tiers` | constraint | `manual` mode shall require permission before every write and terminal command. | A manual-mode worker cannot write or run commands without approval. | `Agent`; `AgentMode` | Layer 1; Layer 3 | Ambiguous relative to proto `AGENT_MODE_NORMAL`. |
| `REQ-ORCH-006` | `sec-06-autonomy-tiers` | constraint | `plan` mode shall require the agent to propose a plan and the daemon to pause for operator approval via gRPC before execution. | A plan-mode worker waits for approval after planning and before execution. | `Agent`; `AgentMode`; `OperatorCommand` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-ORCH-007` | `sec-06-autonomy-tiers` | constraint | `full_auto` mode shall run until task completion, budget limit, or error without human gates. | A full-auto worker proceeds until completion or a terminating condition occurs. | `Agent`; `AgentMode` | Layer 1 | — |
| `REQ-ORCH-008` | `sec-06-uncertainty-routing-loop-detection` | capability | If an agent repeats the same test-fail-write-test loop three times in a row, the Orchestrator shall halt that agent process. | A three-iteration identical loop causes an immediate stop of the affected agent. | `Agent`; `OrchestratorAgent` | Layer 1 | — |
| `REQ-ORCH-009` | `sec-06-uncertainty-routing-loop-detection` | capability | Loop detection shall emit an `UncertaintyFlagTriggered` event and pulse the affected grid cell orange without halting the rest of the swarm. | The event appears and only the implicated agent is paused/flagged. | `UncertaintyFlagTriggered`; `Agent`; `Synapse Telemetry Grid` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-ORCH-010` | `sec-06-observer-agent-three-monitoring-loops` | security | The Static Enforcement Loop shall use OS-level file watchers to enforce per-slot context rules. | Writes outside allowed context are detected by the observer. | `AgentSlot`; `Static Enforcement Loop`; `Agent` | Layer 0; Layer 1 | — |
| `REQ-ORCH-011` | `sec-06-observer-agent-three-monitoring-loops` | security | If an agent writes to a path excluded by `context.exclude`, the Observer shall block the write and inject a mock terminal error. | An excluded-path write is prevented and surfaced as a terminal failure. | `Agent`; `AgentSlot` | Layer 0; Layer 1 | — |
| `REQ-ORCH-012` | `sec-06-observer-agent-three-monitoring-loops` | capability | The Dynamic Loop shall monitor stdout/stderr for repeated identical tool calls and anomalous diff sizes. | Tool repetition and unusual diffs are detectable by the runtime observer. | `Agent`; `Dynamic Loop` | Layer 1 | — |
| `REQ-ORCH-013` | `sec-06-observer-agent-three-monitoring-loops` | capability | The Dynamic Loop shall trigger `Stop the Line` after a configurable threshold. | Crossing the configured threshold pauses or stops the implicated work. | `Dynamic Loop`; `OrchestratorAgent` | Layer 1 | The exact threshold is configurable, not fixed here. |
| `REQ-ORCH-014` | `sec-06-observer-agent-three-monitoring-loops` | deferred | The Semantic Loop shall use periodic background LLM calls in Phase 4 to detect semantic drift and trigger correction or pause. | Phase 4 can detect when a worker is doing the wrong task and react. | `Semantic Loop`; `Agent`; `AgentSlot` | Layer 1 | Phase-gated to Phase 4. |
| `REQ-ORCH-015` | `sec-06-per-project-cost-enforcement` | invariant | Cost accounting shall exist at four levels: per-agent, per-slot, per-project, and per-session. | Cost can be queried or enforced at all four scopes. | `Agent`; `AgentSlot`; `Project`; `NexodeSession` | Layer 1 | — |
| `REQ-ORCH-016` | `sec-06-per-project-cost-enforcement` | capability | When a project's cost exceeds `budget.max_usd`, the daemon shall send `SIGTERM` to all agents in that project and emit `ProjectBudgetAlert` with `hard_kill = true`. | Crossing a project max threshold terminates all project agents and emits a hard budget alert. | `Project`; `Agent`; `ProjectBudgetAlert` | Layer 0; Layer 1; Layer 2; Layer 3 | — |
| `REQ-ORCH-017` | `sec-06-per-project-cost-enforcement` | capability | The warning threshold shall emit `ProjectBudgetAlert` with `hard_kill = false`. | Crossing a warning threshold produces a soft budget alert without hard stop semantics. | `Project`; `ProjectBudgetAlert` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-ORCH-018` | `sec-06-token-accounting-schema` | capability | The token accounting schema shall include a `token_log` table keyed by session, project, slot, agent, model, token counts, and USD cost. | Token logs persist all listed accounting fields in SQLite. | `Token Accountant`; `Agent`; `AgentSlot`; `Project`; `NexodeSession` | Layer 1 | — |
| `REQ-ORCH-019` | `sec-06-token-accounting-schema` | capability | The token accounting schema shall include a `project_costs` view aggregating token totals and cost by `project_id`. | Project-level aggregate cost queries run directly against the view. | `Project`; `Token Accountant` | Layer 1 | — |
| `REQ-ORCH-020` | `sec-06-token-accounting-schema` | capability | The token accounting schema shall include a `slot_costs` view aggregating cost by `project_id` and `slot_id`. | Slot-level aggregate cost queries run directly against the view. | `Project`; `AgentSlot`; `Token Accountant` | Layer 1 | — |

## Phased Delivery

| Requirement ID | Spec section ID | Type | Requirement statement | Observable success condition | Related domain entities | Architectural layers touched | Notes on ambiguity |
|---|---|---|---|---|---|---|---|
| `REQ-PLAN-001` | `sec-07-phased-development-timeline-overview` | constraint | The roadmap shall prove the daemon and headless orchestration before investing in complex UI. | Implementation work reaches headless orchestration milestones before UI-heavy phases. | `Hypervisor`; `OrchestratorAgent`; `Rendering Shell` | Layer 1; Layer 3 | — |
| `REQ-PLAN-002` | `sec-07-phased-development-timeline-overview` | constraint | Each phase shall define explicit success criteria and kill criteria. | Each named phase includes explicit exit or kill conditions in the spec. | `Hypervisor`; `Rendering Shell` | All layers | — |
| `REQ-PLAN-003` | `sec-07-v2-scope-change-e-007` | deferred | Agent Pools, `.swarm/` protocol, mutation zones, pool persona YAML, and intra-pool DAG shall be removed from Phase 0-1 scope and deferred to Phase 3+. | Phase 0-1 implementation excludes the named pool features. | `Agent Pool`; `.swarm/`; `OrchestratorAgent` | Layer 1; Layer 3 | — |
| `REQ-PLAN-004` | `sec-07-v2-scope-change-e-007` | capability | Phase 0-1 scope shall instead focus on the multi-project `session.yaml v2` parser, `AgentSlot` abstraction, per-project cost tracking, and budget enforcement. | Phase 0-1 deliverables align with the replacement scope items. | `NexodeSession`; `AgentSlot`; `Project`; `Token Accountant` | Layer 1 | — |
| `REQ-P0-001` | `sec-08-phase-0-spike-validate` | capability | Phase 0 shall prove multi-project `session.yaml` parsing, multi-repo agent spawning, per-project cost tracking, and budget enforcement in a headless-only daemon. | The Phase 0 demos and tests show all four capabilities without any TUI or VS Code dependency. | `NexodeSession`; `Project`; `Agent`; `AgentSlot`; `Token Accountant` | Layer 1 | — |
| `REQ-P0-002` | `sec-08-week-1-foundation` | capability | The Phase 0 parser shall deserialize the full `session.yaml v2` schema. | Parsing tests succeed for the full schema. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-P0-003` | `sec-08-week-1-foundation` | constraint | Phase 0 parsing shall implement defaults cascade `session > project > slot`, include resolution, and v1 auto-wrap. | Tests pass for defaults cascade, includes, and legacy wrapping. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1 | Conflicts with the formal schema, which does not define a project-level defaults block. |
| `REQ-P0-004` | `sec-08-week-1-foundation` | capability | Phase 0 domain types shall include `NexodeSession`, `Project`, and `AgentSlot` as Rust structs with builder pattern support. | The domain model compiles and builder-based construction is available in tests/code. | `NexodeSession`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-P0-005` | `sec-08-week-1-foundation` | capability | Unit tests shall cover `slot_empty > slot_assigned > agent_crashed > slot_reassigned` transitions. | The listed state-transition tests pass. | `AgentSlot`; `Agent` | Layer 1 | — |
| `REQ-P0-006` | `sec-08-week-1-foundation` | capability | Phase 0 shall implement `token_log` with `project_id` and `slot_id`, plus the `project_costs` view, and verify aggregation with mock token events. | SQLite tests confirm inserts and aggregate queries behave as specified. | `Token Accountant`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-P0-007` | `sec-08-week-1-foundation` | capability | Phase 0 shall parse the `models` block from `session.yaml`, feed pricing into the Token Accountant, and verify USD estimation. | `estimate_cost(model, tokens_in, tokens_out)` returns correct USD in tests. | `Token Accountant`; `NexodeSession` | Layer 1 | — |
| `REQ-P0-008` | `sec-08-week-2-agent-lifecycle` | capability | Given 3 projects across 3 repos, the daemon shall create git worktrees for each slot on the correct branch in the correct repo. | Integration tests verify worktree location and branch correctness for all slots. | `Project`; `AgentSlot`; `Worktree` | Layer 0; Layer 1 | — |
| `REQ-P0-009` | `sec-08-week-2-agent-lifecycle` | capability | Phase 0 shall spawn real `claude-code` or `codex` processes inside worktrees, capture stdout/stderr, detect process exit, and log token events to SQLite. | Integration tests show live agent lifecycle handling and token logging. | `Agent`; `Worktree`; `Token Accountant` | Layer 0; Layer 1 | — |
| `REQ-P0-010` | `sec-08-week-2-agent-lifecycle` | capability | Per-project budget enforcement shall `SIGTERM` all project agents at `budget.max_usd`, and per-session enforcement shall provide a global hard kill. | Tests with mock pricing show both project-level and session-level stop behavior. | `Project`; `NexodeSession`; `Agent` | Layer 0; Layer 1 | — |
| `REQ-P0-011` | `sec-08-week-2-agent-lifecycle` | capability | Killing an agent with `SIGKILL` shall cause the daemon to respawn a replacement into the same slot while preserving slot ID and cost continuity. | Integration tests verify same-slot recovery and uninterrupted slot-level cost accrual. | `Agent`; `AgentSlot`; `Token Accountant` | Layer 0; Layer 1 | — |
| `REQ-P0-012` | `sec-08-phase-0-exit-criteria` | performance | Phase 0 exit shall require a demo that spawns 5 agents across 3 repos from one `session.yaml`. | The demo runs successfully with the named scale. | `NexodeSession`; `Project`; `Agent` | Layer 1 | — |
| `REQ-P0-013` | `sec-08-phase-0-exit-criteria` | capability | Phase 0 exit shall require a demo of slot recovery and cost continuity after killing an agent. | The recovery demo shows the same slot continuing after agent death. | `Agent`; `AgentSlot`; `Token Accountant` | Layer 1 | — |
| `REQ-P0-014` | `sec-08-phase-0-exit-criteria` | capability | Phase 0 exit shall require a demo of project-level shutdown at a project budget ceiling. | A project budget breach shuts down that project's agents during the demo. | `Project`; `Agent` | Layer 1 | — |
| `REQ-P0-015` | `sec-08-phase-0-exit-criteria` | capability | Phase 0 exit shall require all token events to be recorded in SQLite with correct `project_id` and `slot_id`, and CI acceptance tests shall pass via `cargo test`. | SQLite verification and CI both succeed. | `Token Accountant`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-P0-016` | `sec-08-kill-criteria` | constraint | Phase 0 shall stop if multi-repo git worktree creation is flaky, if tokio cannot gracefully kill runaway processes across OS environments, or if the merge step consistently produces broken code. | Any one of those failure modes blocks progression beyond Phase 0. | `Worktree`; `Agent`; `Hypervisor` | Layer 0; Layer 1 | The merge-step criterion is underspecified elsewhere in Phase 0. |
| `REQ-P1-001` | `sec-09-phase-1-headless-orchestrator` | capability | Phase 1 shall expand the daemon to manage 10-15 agents across 10-15 projects with full headless orchestration. | Headless orchestration operates at the named scale without a TUI. | `NexodeSession`; `Project`; `Agent`; `AgentSlot` | Layer 1 | — |
| `REQ-P1-002` | `sec-09-phase-1-headless-orchestrator` | capability | Phase 1 shall add gRPC streaming, WAL-based crash recovery, and the OrchestratorAgent core loop, while still validating through gRPC client and CLI rather than TUI. | Phase 1 runs headlessly with the named components and validation path. | `Hypervisor`; `OrchestratorAgent`; `WAL`; `nexode-ctl` | Layer 1; Layer 2 | — |
| `REQ-P1-003` | `sec-09-weeks-3-4-grpc-state` | capability | Phase 1 shall implement `SubscribeEvents`, `DispatchCommand`, and `GetFullState`. | A gRPC client can invoke all three methods successfully. | `Hypervisor`; `OperatorCommand`; `FullStateSnapshot` | Layer 1; Layer 2 | — |
| `REQ-P1-004` | `sec-09-weeks-3-4-grpc-state` | capability | Phase 1 states that `FullStateSnapshot` shall include `projects[]` and `agent_slots[]`. | A snapshot response contains both collections. | `FullStateSnapshot`; `Project`; `AgentSlot` | Layer 1; Layer 2 | Conflicts with the earlier proto definition, which includes `projects` and `task_dag` but not `agent_slots[]`. |
| `REQ-P1-005` | `sec-09-weeks-3-4-grpc-state` | capability | Phase 1 event streaming shall include `ProjectBudgetAlert` and `SlotAgentSwapped`. | A client receives those event types over the live stream. | `ProjectBudgetAlert`; `SlotAgentSwapped` | Layer 1; Layer 2 | — |
| `REQ-P1-006` | `sec-09-weeks-3-4-grpc-state` | capability | WAL persistence shall store `NexodeSession` state to disk and, after a daemon crash/restart, reload the session and either re-attach live agents or respawn into slots. | Restart tests recover session state and active work through WAL. | `NexodeSession`; `Agent`; `AgentSlot`; `WAL` | Layer 1 | — |
| `REQ-P1-007` | `sec-09-weeks-3-4-grpc-state` | capability | The daemon shall scan each project's repo root for `.nexode.yaml` and merge repo-local slots so that matching IDs win locally. | Repo-local overlays are discovered automatically and merge as specified. | `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-P1-008` | `sec-09-weeks-3-4-grpc-state` | capability | Phase 1 shall provide `nexode-ctl` as a Rust CLI that connects to the daemon, displays state, and sends commands including `status`, `kill`, and `reassign`. | The CLI can connect, show state, and send the named commands. | `nexode-ctl`; `Hypervisor`; `OperatorCommand` | Layer 1; Layer 2 | — |
| `REQ-P1-009` | `sec-09-weeks-5-6-orchestration` | capability | The OrchestratorAgent core loop shall run as a tokio task, mark completed slots idle with an emitted event, and auto-spawn when a slot has no agent. | Slot completion and empty-slot conditions produce the described state transitions. | `OrchestratorAgent`; `AgentSlot`; `Agent`; `HypervisorEvent` | Layer 1; Layer 2 | — |
| `REQ-P1-010` | `sec-09-weeks-5-6-orchestration` | constraint | Phase 1 orchestration shall remain per-project only with no cross-project intelligence. | Dispatch and recovery decisions do not span project boundaries in Phase 1. | `OrchestratorAgent`; `Project`; `AgentSlot` | Layer 1 | — |
| `REQ-P1-011` | `sec-09-weeks-5-6-orchestration` | capability | Phase 1 shall implement `manual`, `plan`, and `full_auto` autonomy tiers per slot, with plan-mode approval over gRPC and full-auto running until completion or budget exceedance. | Slot-level autonomy mode changes behave as specified in integration testing. | `AgentSlot`; `Agent`; `OperatorCommand` | Layer 1; Layer 2 | Ambiguous relative to proto `AGENT_MODE_NORMAL`. |
| `REQ-P1-012` | `sec-09-weeks-5-6-orchestration` | capability | Phase 1 uncertainty routing shall pause an agent and emit `AGENT_STATE_BLOCKED` when stdout shows repeated identical tool calls more than three times. | Repetition beyond the threshold results in a blocked state and operator intervention path. | `Agent`; `AgentState`; `OrchestratorAgent` | Layer 1; Layer 2 | — |
| `REQ-P1-013` | `sec-09-weeks-5-6-orchestration` | capability | The basic Phase 1 context compiler shall build context from task description, include/exclude file lists, and recent git diff, without AST analysis, and inject it as system prompt or `CLAUDE.md`. | Dispatches include the named context sources and omit AST-derived context. | `AgentSlot`; `Scoped Context Compiler`; `Worktree` | Layer 0; Layer 1 | — |
| `REQ-P1-014` | `sec-09-weeks-7-8-buffer-hardening` | performance | Phase 1 hardening shall include scale testing with 15 agents across 10 projects on the DGX Spark target. | The scale test completes at the named scale on the target hardware. | `Agent`; `Project`; `DGX Spark` | Layer 1 | — |
| `REQ-P1-015` | `sec-09-weeks-7-8-buffer-hardening` | performance | Phase 1 hardening shall keep daemon RSS under 200MB with 15 agent processes. | Memory profiling shows daemon RSS below 200MB at the stated load. | `Hypervisor`; `Agent` | Layer 1 | — |
| `REQ-P1-016` | `sec-09-weeks-7-8-buffer-hardening` | performance | Phase 1 hardening shall stress-test 15 multiplexed agent event streams to 3 concurrent clients. | All three clients receive multiplexed events successfully at the stated scale. | `HypervisorEvent`; `Rendering Shell` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-P1-017` | `sec-09-weeks-7-8-buffer-hardening` | capability | Phase 1 hardening shall include README, `session.yaml` reference, `nexode-ctl` man page, `cargo test`, and an integration suite with mock agents. | Documentation exists and both unit/integration suites pass. | `nexode-ctl`; `NexodeSession`; `Agent` | Layer 1 | — |
| `REQ-P1-018` | `sec-09-phase-1-exit-criteria` | capability | Phase 1 exit shall require a demo of 10 agents across 8 projects managed headlessly by `nexode-ctl`, WAL recovery after daemon restart, plan-mode approval via gRPC, loop detection causing `BLOCKED`, and passing CI. | All listed Phase 1 exit demonstrations and CI checks succeed. | `nexode-ctl`; `WAL`; `Agent`; `Project`; `AgentState` | Layer 1; Layer 2 | — |
| `REQ-P1-019` | `sec-09-phase-0-1-dependency-graph` | constraint | Phase 0-1 work items on the same dependency-graph row shall be parallelizable, and later rows shall depend on earlier rows. | Planning and execution can schedule same-row work in parallel without violating dependency order. | `Workflow DAG Engine`; `NexodeSession` | Layer 1 | Applies to implementation sequencing rather than runtime behavior. |
| `REQ-P1-020` | `sec-09-risk-register` | capability | To mitigate CLI compatibility risk, the implementation shall provide an `AgentHarness` abstraction and test it against at least two real CLIs in Phase 0. | Harness tests pass against two or more actual CLI providers. | `Agent Harness abstraction`; `Agent` | Layer 1 | — |
| `REQ-P1-021` | `sec-09-risk-register` | constraint | To mitigate parser edge cases, parsing shall use serde strict mode, JSON Schema validation before parse, and extensive tests for includes, defaults cascade, and v1 fallback. | Invalid schema/config is rejected early and targeted tests pass. | `Session Config Manager`; `NexodeSession` | Layer 1 | Defaults cascade note inherits the project-default ambiguity. |
| `REQ-P1-022` | `sec-09-risk-register` | capability | To mitigate pricing drift, the pricing table shall remain user-editable and shall warn when older than 30 days. | A stale pricing table produces a visible warning. | `Token Accountant`; `NexodeSession` | Layer 1; Layer 3 | The spec labels provider auto-fetch as future work, not a current requirement. |
| `REQ-P1-023` | `sec-09-risk-register` | capability | DGX Spark compatibility work shall test early on ARM. | ARM-target validation runs before late-stage delivery. | `DGX Spark`; `Hypervisor` | Layer 1 | — |
| `REQ-P2-001` | `sec-10-phase-2-tui-command-center` | UX | Phase 2 shall deliver a rich terminal interface that gives one developer real-time visibility and control over 10+ concurrent agents across multiple projects. | The TUI operates as the primary control surface at the named scale. | `TUI`; `Agent`; `Project` | Layer 3 | — |
| `REQ-P2-002` | `sec-10-v2-changes-e-008` | UX | Phase 2 TUI shall support project-level grouping, `Tab`/`Shift+Tab` project cycling, per-project views, and `/` fuzzy search across all agents and projects. | Operators can navigate and search across projects through the named controls. | `TUI`; `Project`; `Agent` | Layer 3 | — |
| `REQ-P2-003` | `sec-10-ux-ui-layout` | UX | The Phase 2 TUI shall auto-tile the agent grid based on terminal width and active agent count, show per-agent panes, apply the named status colors, and provide a collapsible project sidebar with counts and cost summaries. | The TUI layout and visual state match the described grouping, pane content, colors, and sidebar behavior. | `TUI`; `Agent`; `Project` | Layer 3 | — |
| `REQ-P2-004` | `sec-10-interactive-controls` | UX | The Phase 2 TUI shall provide project navigation (`Tab`, `Shift+Tab`, `1-9`, `Enter`, `Esc`), agent navigation (`h`, `j`, `k`, `l`), command overlay controls (`c`), diff viewer toggle (`d`), and global fuzzy search (`/`). | Operators can invoke all named keyboard controls successfully. | `TUI`; `Agent`; `Project`; `Worktree` | Layer 3 | — |
| `REQ-P2-005` | `sec-10-agent-harness-abstraction` | capability | Phase 2 shall use trait-based harness adapters for Claude Code, Codex CLI, and Gemini CLI so different models can run in one session, configured through `.nexode.yaml` or session defaults. | One session can run at least the three named provider types using harness-backed config. | `Agent Harness abstraction`; `Agent`; `NexodeSession` | Layer 1; Layer 3 | — |
| `REQ-P2-006` | `sec-10-session-persistence` | capability | If the terminal closes, the TUI shall recover from the SQLite state database and WAL, re-attach to running daemon processes, and resume orchestration where it left off. | Restarting the TUI after terminal closure returns the operator to the prior live session state. | `TUI`; `WAL`; `Token Accountant`; `Agent` | Layer 1; Layer 3 | — |
| `REQ-P2-007` | `sec-10-phase-2-success-kill-criteria` | performance | Phase 2 success shall require one developer managing 10+ agents across multiple projects from one terminal and at least three different agent types operating through the harness abstraction. | Both the concurrency scale and provider diversity targets are met in demonstration/testing. | `TUI`; `Agent`; `Project`; `Agent Harness abstraction` | Layer 1; Layer 3 | — |
| `REQ-P2-008` | `sec-10-phase-2-success-kill-criteria` | constraint | If rendering 10+ active streams causes ratatui lag or excessive CPU, optimization shall focus on local state caching before attempting a gRPC bridge to VS Code. | Performance remediation prioritizes local caching over prematurely moving to a different UI transport. | `TUI` | Layer 3 | — |
| `REQ-P3-001` | `sec-11-phase-3-vscode-integration` | capability | Phase 3 shall connect the Rust daemon to VS Code through an extension-first approach that delivers Synapse Grid, Kanban DAG, and native chat routing. | The VS Code extension renders the named surfaces and routes commands to the daemon. | `Hypervisor`; `Synapse Telemetry Grid`; `TaskNode`; `ChatParticipant` | Layer 1; Layer 2; Layer 3 | — |
| `REQ-P3-002` | `sec-11-week-1-grpc-bridge-state-cache` | capability | Phase 3 shall use `@grpc/grpc-js` over a local Unix domain socket, with a named pipe on Windows. | The VS Code extension connects to the daemon on both Unix-like and Windows systems. | `Hypervisor`; `Rendering Shell` | Layer 2; Layer 3 | — |
| `REQ-P3-003` | `sec-11-week-1-grpc-bridge-state-cache` | capability | Phase 3 shall maintain `StateCache` as an in-memory replica of `FullStateSnapshot`. | The extension can reconstruct current state from the local cache without re-querying on every render. | `StateCache`; `FullStateSnapshot` | Layer 3 | — |
| `REQ-P3-004` | `sec-11-week-1-grpc-bridge-state-cache` | capability | The Phase 3 `EventBus` shall collect events by `barrier_id`, update `StateCache`, send one unified `postMessage` to all React Webviews, and hold new events until all active Webviews ACK. | No webview advances past a barrier until all active webviews acknowledge that barrier. | `EventBus`; `HypervisorEvent`; `StateCache`; `Rendering Shell` | Layer 2; Layer 3 | — |
| `REQ-P3-005` | `sec-11-weeks-2-4-multi-monitor-react-webviews` | UX | Synapse Grid shall use `vscode.window.createWebviewPanel`, a virtualized `react-grid-layout`, and `useRef` plus direct DOM mutation for 60fps terminal streams. | The grid renders live streams smoothly and at the intended frame rate. | `Synapse Telemetry Grid`; `Rendering Shell` | Layer 3 | — |
| `REQ-P3-006` | `sec-11-weeks-2-4-multi-monitor-react-webviews` | capability | The Kanban/DAG surface shall use `reactflow`, and drag-and-drop task assignment shall trigger the `AssignTask` gRPC command. | Moving a task in the DAG causes the corresponding daemon command to be sent. | `TaskNode`; `AssignTask`; `Workflow DAG Engine` | Layer 2; Layer 3 | — |
| `REQ-P3-007` | `sec-11-weeks-5-6-native-vscode-integrations` | UX | Phase 3 shall register `vscode.chat.createChatParticipant('nexode.orchestrator', handler)` and route `@nexode` commands over gRPC. | Chat commands issued in VS Code reach the daemon through the participant. | `ChatParticipant`; `ChatDispatch`; `OrchestratorAgent` | Layer 2; Layer 3 | — |
| `REQ-P3-008` | `sec-11-weeks-5-6-native-vscode-integrations` | UX | Phase 3 shall provide a compressed Synapse Grid in a sidebar `WebviewViewProvider`, a pop-out control, Merge Choreography diff integration, and a HUD StatusBar that updates every 1000ms. | All named VS Code integrations exist and update at the named cadence. | `Synapse Telemetry Grid`; `Merge Choreography`; `Status Bar HUD` | Layer 3 | Merge Choreography again refers to worktrees in `REVIEW`, which is not a defined Worktree state. |
| `REQ-P3-009` | `sec-11-week-7-worktree-router` | capability | The `Take Over` command shall add a blocked agent's hidden worktree to the VS Code workspace via `updateWorkspaceFolders()` so the developer can fix it and resume the agent. | Clicking a blocked agent exposes its worktree in the workspace and the agent can later be resumed. | `Worktree Router`; `Worktree`; `Agent` | Layer 0; Layer 3 | — |
| `REQ-P3-010` | `sec-11-phase-3-success-kill-criteria` | performance | Phase 3 success shall require the EventBus to process 1,000 token updates/sec across 15 agents without editor freeze, with webviews staying in sync across monitors and UI events routed natively. | Load testing confirms throughput, no freeze, cross-monitor sync, and native routing. | `EventBus`; `HypervisorEvent`; `Synapse Telemetry Grid`; `OrchestratorAgent` | Layer 2; Layer 3 | — |
| `REQ-P3-011` | `sec-11-phase-3-success-kill-criteria` | constraint | If Extension Host IPC cannot handle telemetry volume despite optimized `useRef` rendering and barrier sync, Phase 3 shall halt and trigger Phase 5. | The Deep Fork trigger is activated when the named failure condition is observed. | `Extension Host`; `EventBus` | Layer 3 | — |
| `REQ-P4-001` | `sec-12-phase-4-smart-context-semantic-memory` | capability | Phase 4 shall transition prompting from file-centric context to AST-aware semantic context injection. | Dispatch context is built from structural and historical context rather than file dumps alone. | `Scoped Context Compiler`; `Codebase Graph`; `Decision Log` | Layer 1 | — |
| `REQ-P4-002` | `sec-12-phase-4-smart-context-semantic-memory` | performance | Phase 4 shall target 20-40% token reduction. | Token consumption measurements show a reduction within the stated range. | `Scoped Context Compiler`; `Token Accountant` | Layer 1 | Also stated in Section 3.3. |
| `REQ-P4-003` | `sec-12-step-1-real-time-ast-indexing` | capability | The daemon shall parse the entire codebase into AST on initialization and update the AST incrementally via file watchers as agents mutate code. | AST state exists on init and remains up to date as files change. | `Codebase Graph`; `Project`; `Agent` | Layer 0; Layer 1 | — |
| `REQ-P4-004` | `sec-12-step-1-real-time-ast-indexing` | capability | Signature extraction shall strip function bodies and retain structural signatures such as interfaces, types, class declarations, and method headers. | Retrieved structural context excludes function bodies and contains signatures only. | `Codebase Graph` | Layer 1 | — |
| `REQ-P4-005` | `sec-12-step-2-lancedb-vector-memory-bus` | capability | Phase 4 shall embed AST signatures into embedded LanceDB and shall embed and persist every agent reasoning and architectural decision. | Both structural code signatures and decision history are queryable from LanceDB-backed storage. | `LanceDB`; `Codebase Graph`; `Decision Log`; `Agent` | Layer 1 | — |
| `REQ-P4-006` | `sec-12-step-3-scoped-context-compiler` | capability | Each slot dispatch shall compile three inputs: role persona, structural AST context from LanceDB, and historical intent from the Decision Log. | Dispatched prompt payloads include all three named context sources. | `AgentSlot`; `Scoped Context Compiler`; `Decision Log`; `Codebase Graph` | Layer 1 | — |
| `REQ-P4-007` | `sec-12-step-4-predictive-conflict-routing` | capability | Before merging worktrees, the daemon shall compare AST mutations and flag high structural conflict risk when semantically dangerous merge patterns are detected. | Merge-risk analysis identifies dangerous-but-textually-mergeable changes before merge. | `Worktree`; `Predictive Conflict Routing`; `TaskNode` | Layer 0; Layer 1 | — |
| `REQ-P4-008` | `sec-12-step-4-predictive-conflict-routing` | UX | A high structural conflict risk shall turn the merge node red in the UI. | Merge nodes change to red when the risk is flagged. | `TaskNode`; `Merge Choreography`; `Worktree` | Layer 1; Layer 3 | — |
| `REQ-P4-009` | `sec-12-phase-4-success-kill-criteria` | performance | Phase 4 success shall require 20%+ token reduction, successful dependent-task completion using only the Decision Log, and no noticeable latency added to the Orchestrator tick loop. | Performance and behavior tests meet all three stated targets. | `Decision Log`; `Scoped Context Compiler`; `OrchestratorAgent` | Layer 1 | — |
| `REQ-P4-010` | `sec-12-phase-4-success-kill-criteria` | constraint | If embeddings and LanceDB updates cause runtime stutter or missed UI telemetry, vector DB work shall be offloaded to a separate microservice or update frequency shall be throttled. | The mitigation path is invoked when vector updates degrade runtime responsiveness. | `LanceDB`; `HypervisorEvent`; `OrchestratorAgent` | Layer 1; Layer 2 | — |
| `REQ-P5-001` | `sec-13-phase-5-deep-fork-conditional` | deferred | Phase 5 shall only trigger if the Phase 3 extension-based approach definitively fails because Extension Host IPC cannot handle 15 simultaneous token streams or native UI capability limits are hit. | The deep fork does not start unless one of the stated failure conditions is confirmed. | `Extension Host`; `Rendering Shell` | Layer 3 | — |
| `REQ-P5-002` | `sec-13-architectural-shift-bypassing-extension-host` | deferred | Phase 5 shall move `HypervisorClient` and `EventBus` into VS Code core as `IHypervisorService` to eliminate extension-host serialization overhead and provide direct synchronous access to internal VS Code APIs. | A forked VS Code core integrates the hypervisor service directly and removes extension-host serialization from the hot path. | `HypervisorClient`; `EventBus`; `IHypervisorService` | Layer 3 | — |
| `REQ-P5-003` | `sec-13-ui-overhaul-native-dom` | deferred | Phase 5 shall remove React Webviews and rewrite Synapse Grid and Kanban using VS Code native DOM primitives, targeting 60fps with near-zero memory overhead. | The forked UI renders through native DOM paths and meets the stated frame/memory goals. | `Synapse Telemetry Grid`; `Macro Kanban Board` | Layer 3 | — |
| `REQ-P5-004` | `sec-13-exploiting-sessions-layer` | deferred | Phase 5 shall expand `AgenticParts` to include `SynapseGridPart` and `MacroKanbanPart` and take control of `ViewContainerLocation.ChatBar`. | The forked sessions layer exposes the named parts and chat-bar ownership. | `AgenticParts`; `SynapseGridPart`; `MacroKanbanPart` | Layer 3 | — |
| `REQ-P5-005` | `sec-13-native-worktree-scm-isolation` | deferred | Phase 5 shall rewrite workspace and SCM logic so Explorer distinguishes `Human Main Codebase` from `Agent Sandboxes`, and clicking an agent shall context-switch the VS Code window to that agent's git worktree. | The editor clearly separates human and agent sandboxes and can switch into an agent worktree from the grid. | `Worktree`; `Agent`; `Rendering Shell` | Layer 0; Layer 3 | — |
| `REQ-P5-006` | `sec-13-maintenance-strategy` | constraint | Phase 5 maintenance shall avoid heavily mutating core VS Code files, keep Hypervisor logic under `src/vs/hypervisor/`, use patch tracking for minimal hooks, and automate monthly rebases. | The forked codebase keeps the hypervisor isolated and uses the stated patch/rebase discipline. | `Hypervisor` | Layer 3 | — |

## Deferred Pool Architecture and Licensing

| Requirement ID | Spec section ID | Type | Requirement statement | Observable success condition | Related domain entities | Architectural layers touched | Notes on ambiguity |
|---|---|---|---|---|---|---|---|
| `REQ-POOL-001` | `app-a-agent-pools-swarm-protocol` | deferred | Appendix A pool architecture shall be deferred from the MVP and Phase 0-1 scope to Phase 3+. | No pool-specific implementation is required in Phase 0-1. | `Agent Pool`; `.swarm/` | Layer 1; Layer 3 | — |
| `REQ-POOL-002` | `app-a-agent-pools-swarm-protocol` | deferred | When implemented, a Sprint Pool shall assign 1-4 agents to a single Macro Kanban card. | A future pool configuration can attach 1-4 agents to one macro task. | `Agent Pool`; `TaskNode` | Layer 1; Layer 3 | Deferred feature. |
| `REQ-POOL-003` | `app-a-pool-structure-role-based-micro-swarms` | deferred | Pool roles shall separate Builder implementation work, Tester test-writing work, and Reviewer/Documenter review/documentation work. | A pool can assign those distinct responsibilities to different agents. | `Agent Pool`; `Agent` | Layer 1 | Deferred feature. |
| `REQ-POOL-004` | `app-a-swarm-file-based-message-bus` | deferred | Pooled agents shall share a `.hypervisor/worktrees/pool-A/.swarm/` directory, the Builder shall write status to `.swarm/builder_status.md`, and the daemon shall watch that directory to alert the Tester. | File-based pool coordination works through the named directory and builder status file. | `.swarm/`; `Agent Pool`; `Agent`; `OrchestratorAgent` | Layer 0; Layer 1 | Deferred feature. |
| `REQ-POOL-005` | `app-a-coordination-constraints` | deferred | Pool mutation zones shall enforce directory-level write locks so Builder owns `/src`, Tester owns `/tests`, and Documenter owns `.md` files. | Pooled agents cannot mutate files outside their assigned mutation zones. | `Agent Pool`; `Agent` | Layer 0; Layer 1 | Deferred feature. |
| `REQ-POOL-006` | `app-a-coordination-constraints` | deferred | Pool workflow shall enforce strict ordering so Tester cannot start until Builder commits the interface, and test failure shall assign a bug sub-task back to Builder through `.swarm/`. | Pool sequencing blocks premature testing and routes failures back to Builder. | `Agent Pool`; `TaskNode`; `.swarm/` | Layer 0; Layer 1 | Deferred feature. |
| `REQ-POOL-007` | `app-a-coordination-constraints` | deferred | If Builder and Tester loop on the same write/fail pattern, OrchestratorAgent shall pause the entire pool and flag the UI. | A pool-wide stop occurs when the named loop pattern is detected. | `Agent Pool`; `OrchestratorAgent`; `Agent` | Layer 1; Layer 3 | Deferred feature. |
| `REQ-LIC-001` | `app-b-licensing-distribution-strategy` | licensing | The Rust daemon and core libraries shall be licensed under MIT / Apache 2.0 as Tier 0. | Packaging and published license metadata identify Tier 0 accordingly. | `Hypervisor` | Layer 1 | — |
| `REQ-LIC-002` | `app-b-licensing-distribution-strategy` | licensing | The TUI and TUI plugin system shall be licensed under MIT / Apache 2.0 as Tier 0.5. | Packaging and published license metadata identify Tier 0.5 accordingly. | `TUI` | Layer 3 | — |
| `REQ-LIC-003` | `app-b-licensing-distribution-strategy` | licensing | The VS Code extension pack shall be source-available / BSL as Tier 1, with a free tier capped at 3 agents and 1 project and a paid tier offering unlimited agents/projects plus advanced features. | Tier 1 packaging and entitlements match the stated free and paid limits. | `Rendering Shell`; `Project`; `Agent` | Layer 3 | — |
| `REQ-LIC-004` | `app-b-licensing-distribution-strategy` | licensing | Enterprise / Cloud features shall be proprietary Tier 2 and include team orchestration, managed API key pooling, audit logs, and SSO. | Tier 2 offerings and license metadata match the proprietary enterprise feature set. | `Rendering Shell`; `Project`; `Agent` | Layer 3 | — |
| `REQ-LIC-005` | `app-b-errata-reference-e-002` | licensing | Bring-your-own-API-key behavior shall belong to Tier 0. | Tier 0 usage path does not require managed key pooling. | `Hypervisor` | Layer 1 | — |
| `REQ-LIC-006` | `app-b-licensing-distribution-strategy` | licensing | Managed API key pooling in Tier 2 shall be a convenience feature rather than a requirement. | Enterprise deployments can operate without mandatory managed key pooling. | `Rendering Shell` | Layer 3 | — |
